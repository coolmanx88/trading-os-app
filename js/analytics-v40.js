import { replayTrade } from './engine.js';

const STATE_KEY='trading-os-state-v1';
const FILTER_KEY='trading-os-analytics-filter-v40';
let lastSignature='';
let renderTimer=null;

const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':'&quot;',"'":"&#39;"}[c]));
const num=v=>{const n=Number(v);return Number.isFinite(n)?n:0};
const money=n=>`${num(n)<0?'-':''}$${Math.abs(num(n)).toLocaleString('en-US',{maximumFractionDigits:2})}`;
const pct=n=>`${num(n).toFixed(1)}%`;
const route=()=>String(location.hash||'#dashboard').slice(1).split('?')[0];
const isoDay=v=>{try{return new Intl.DateTimeFormat('en-CA',{timeZone:'America/New_York',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date(v))}catch{return''}};
const monthKey=d=>String(d||'').slice(0,7);

function loadState(){try{return JSON.parse(localStorage.getItem(STATE_KEY)||'{}')}catch{return{trades:[],accounts:[],companies:[]}}}
function defaults(){return{company:'ALL',period:'ALL',from:'',to:'',instrument:'ALL',direction:'ALL',strategy:'ALL'}}
function loadFilters(){try{return{...defaults(),...JSON.parse(localStorage.getItem(FILTER_KEY)||'{}')}}catch{return defaults()}}
function saveFilters(f){try{localStorage.setItem(FILTER_KEY,JSON.stringify(f))}catch{}}
function entryTs(t){return t.actualEntryAt||(t.events||[]).find(e=>e.type==='INITIAL')?.actualTimestamp||(t.events||[]).find(e=>e.type==='INITIAL')?.timestamp||null}
function exitTs(t){const ev=[...(t.events||[])].reverse().find(e=>e.type==='CLOSE');return t.actualExitAt||ev?.actualTimestamp||ev?.timestamp||t.closedAt||t.createdAt||`${t.date||'1970-01-01'}T12:00:00Z`}
function strategyOf(t){return String(t.strategy||t.setup||t.review?.strategy||t.review?.setup||'Unspecified')}
function companyMatches(t,id){return id==='ALL'||(t.allocations||[]).some(a=>a.companyId===id)}
function rowOf(t){let r;try{r=replayTrade(t)}catch{return null}const entry=entryTs(t),exit=exitTs(t),date=t.date||isoDay(exit),p=num(r.realizedPerAccount),port=num(r.portfolioRealized),duration=entry&&exit?Math.max(0,(new Date(exit)-new Date(entry))/60000):null;return{t,r,p,port,date,entry,exit,duration,strategy:strategyOf(t)}}
function allRows(state){return(state.trades||[]).filter(t=>t.status==='Closed').map(rowOf).filter(Boolean).sort((a,b)=>new Date(a.exit)-new Date(b.exit))}
function periodBounds(f){const now=new Date(),today=isoDay(now);if(f.period==='CUSTOM')return{from:f.from||'',to:f.to||''};if(f.period==='ALL')return{from:'',to:''};let start=new Date();if(f.period==='7D')start.setDate(start.getDate()-6);else if(f.period==='30D')start.setDate(start.getDate()-29);else if(f.period==='90D')start.setDate(start.getDate()-89);else if(f.period==='YTD')start=new Date(Date.UTC(now.getUTCFullYear(),0,1));return{from:isoDay(start),to:today}}
function applyFilters(rows,f){const b=periodBounds(f);return rows.filter(x=>(!b.from||x.date>=b.from)&&(!b.to||x.date<=b.to)&&companyMatches(x.t,f.company)&&(f.instrument==='ALL'||x.t.instrument===f.instrument)&&(f.direction==='ALL'||x.t.direction===f.direction)&&(f.strategy==='ALL'||x.strategy===f.strategy))}
function stats(rows){const wins=rows.filter(x=>x.p>0),losses=rows.filter(x=>x.p<0),gp=wins.reduce((s,x)=>s+x.p,0),gl=Math.abs(losses.reduce((s,x)=>s+x.p,0)),net=rows.reduce((s,x)=>s+x.p,0),port=rows.reduce((s,x)=>s+x.port,0),aw=wins.length?gp/wins.length:0,al=losses.length?gl/losses.length:0,pf=gl?gp/gl:(gp?Infinity:0);let equity=0,peak=0,maxDD=0;const points=[0];for(const x of rows){equity+=x.p;peak=Math.max(peak,equity);maxDD=Math.max(maxDD,peak-equity);points.push(equity)}const durations=rows.map(x=>x.duration).filter(Number.isFinite),avgDuration=durations.length?durations.reduce((a,b)=>a+b,0)/durations.length:null;return{n:rows.length,w:wins.length,l:losses.length,be:rows.length-wins.length-losses.length,net,port,wr:(wins.length+losses.length)?wins.length/(wins.length+losses.length)*100:0,gp,gl,pf,exp:rows.length?net/rows.length:0,aw,al,maxDD,points,avgDuration}}
function fmtDuration(m){if(!Number.isFinite(m))return'—';const h=Math.floor(m/60),min=Math.round(m%60);return h?`${h}h ${min}m`:`${min}m`}
function kpi(label,value,sub='',tone=''){return`<div class="an40-kpi ${tone}"><small>${esc(label)}</small><b>${value}</b>${sub?`<em>${esc(sub)}</em>`:''}</div>`}
function lineSvg(values){if(values.length<2)return'<div class="an40-empty">Not enough closed trades yet.</div>';const W=760,H=210,P=18,lo=Math.min(0,...values),hi=Math.max(0,...values),span=hi-lo||1,x=i=>P+(W-P*2)*i/(values.length-1),y=v=>P+(H-P*2)*(1-(v-lo)/span),path=values.map((v,i)=>`${i?'L':'M'}${x(i).toFixed(1)},${y(v).toFixed(1)}`).join(' '),area=`M${x(0)},${H-P} ${values.map((v,i)=>`L${x(i).toFixed(1)},${y(v).toFixed(1)}`).join(' ')} L${x(values.length-1)},${H-P} Z`;return`<svg class="an40-line-svg" viewBox="0 0 ${W} ${H}" preserveAspectRatio="none"><defs><linearGradient id="an40eq" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stop-color="#16a765" stop-opacity=".28"/><stop offset="100%" stop-color="#16a765" stop-opacity="0"/></linearGradient></defs><path class="fill" d="${area}"/><path class="line" d="${path}"/></svg>`}
function aggregate(rows,keyFn){const m=new Map();for(const x of rows){const k=keyFn(x);if(!k)continue;if(!m.has(k))m.set(k,{key:k,n:0,w:0,p:0,gp:0,gl:0});const z=m.get(k),p=x.p;z.n++;z.p+=p;if(p>0){z.w++;z.gp+=p}else if(p<0)z.gl+=Math.abs(p)}return[...m.values()].map(z=>({...z,wr:z.n?z.w/z.n*100:0,pf:z.gl?z.gp/z.gl:(z.gp?Infinity:0),exp:z.n?z.p/z.n:0}))}
function monthlyBars(rows){const data=aggregate(rows,x=>monthKey(x.date)).slice(-12),max=Math.max(1,...data.map(x=>Math.abs(x.p)));if(!data.length)return'<div class="an40-empty">No monthly data.</div>';return`<div class="an40-bars">${data.map(x=>{const h=Math.max(4,Math.round(Math.abs(x.p)/max*84));return`<div class="an40-bar-col"><div class="an40-bar-zone"><i class="${x.p>=0?'pos':'neg'}" style="height:${h}px" title="${esc(x.key)} · ${money(x.p)}"></i></div><span>${esc(x.key.slice(5))}</span></div>`}).join('')}</div>`}
function comboSvg(rows){const d=aggregate(rows,x=>monthKey(x.date)).slice(-10);if(d.length<2)return'<div class="an40-empty">Need at least two months.</div>';const W=720,H=190,P=20,maxPf=Math.max(2,...d.map(x=>Number.isFinite(x.pf)?x.pf:2)),x=i=>P+(W-P*2)*i/(d.length-1),yPct=v=>P+(H-P*2)*(1-v/100),yPf=v=>P+(H-P*2)*(1-Math.min(v,maxPf)/maxPf),wr=d.map((z,i)=>`${i?'L':'M'}${x(i)},${yPct(z.wr)}`).join(' '),pf=d.map((z,i)=>`${i?'L':'M'}${x(i)},${yPf(Number.isFinite(z.pf)?z.pf:maxPf)}`).join(' ');return`<svg class="an40-combo" viewBox="0 0 ${W} ${H}" preserveAspectRatio="none"><path class="wr" d="${wr}"/><path class="pf" d="${pf}"/>${d.map((z,i)=>`<circle class="wr-dot" cx="${x(i)}" cy="${yPct(z.wr)}" r="3"/><circle class="pf-dot" cx="${x(i)}" cy="${yPf(Number.isFinite(z.pf)?z.pf:maxPf)}" r="3"/>`).join('')}</svg><div class="an40-legend"><span><i class="blue"></i>Win Rate</span><span><i class="green"></i>Profit Factor</span></div>`}
const COLORS=['#2563eb','#16a765','#f59f00','#f76707','#7950f2','#0ca678','#e8590c','#5c7cfa'];
function donut(title,data,total){const sorted=[...data].sort((a,b)=>b.n-a.n).slice(0,6),sum=sorted.reduce((s,x)=>s+x.n,0)||1;let cur=0;const stops=[];sorted.forEach((x,i)=>{const start=cur,end=cur+x.n/sum*360;stops.push(`${COLORS[i%COLORS.length]} ${start}deg ${end}deg`);cur=end});return`<section class="an40-card an40-breakdown"><h3>${esc(title)}</h3><div class="an40-donut-wrap"><div class="an40-donut" style="background:conic-gradient(${stops.join(',')||'#e9eef5 0 360deg'})"><div><b>${total}</b><small>Trades</small></div></div><div class="an40-donut-legend">${sorted.map((x,i)=>`<span><i style="background:${COLORS[i%COLORS.length]}"></i><b>${esc(x.key)}</b><em>${Math.round(x.n/sum*100)}%</em></span>`).join('')||'<span>No data</span>'}</div></div></section>`}
function dailyTable(rows){const d=aggregate(rows,x=>x.date).sort((a,b)=>b.key.localeCompare(a.key)).slice(0,7);return`<section class="an40-card"><div class="an40-card-head"><h3>Daily Performance</h3></div><div class="an40-table-wrap"><table><thead><tr><th>Date</th><th>Trades</th><th>Net P&L</th><th>Win Rate</th></tr></thead><tbody>${d.map(x=>`<tr><td>${esc(x.key)}</td><td>${x.n}</td><td class="${x.p>=0?'pos':'neg'}">${money(x.p)}</td><td>${pct(x.wr)}</td></tr>`).join('')||'<tr><td colspan="4">No data</td></tr>'}</tbody></table></div></section>`}
function tradeRows(rows){return`<table><thead><tr><th>Trade ID</th><th>Instrument</th><th>Net P&L</th><th>Date</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${esc(x.t.id)}</td><td>${esc(x.t.instrument||'—')}</td><td class="${x.p>=0?'pos':'neg'}">${money(x.p)}</td><td>${esc(x.date)}</td></tr>`).join('')||'<tr><td colspan="4">No data</td></tr>'}</tbody></table>`}
function tradesTable(rows){const best=[...rows].sort((a,b)=>b.p-a.p).slice(0,5),worst=[...rows].sort((a,b)=>a.p-b.p).slice(0,5);return`<section class="an40-card"><div class="an40-card-head"><h3>Best & Worst Trades</h3><div class="an40-tabs"><button class="active" data-an40-tab="best">Best</button><button data-an40-tab="worst">Worst</button></div></div><div class="an40-trade-table" data-an40-best>${tradeRows(best)}</div><div class="an40-trade-table hidden" data-an40-worst>${tradeRows(worst)}</div></section>`}
function nyParts(iso){if(!iso)return null;try{const p=Object.fromEntries(new Intl.DateTimeFormat('en-US',{timeZone:'America/New_York',weekday:'short',hour:'2-digit',hour12:false}).formatToParts(new Date(iso)).map(x=>[x.type,x.value]));return{day:p.weekday,h:+p.hour}}catch{return null}}
function session(h){return h<9?'Pre-market':h<10?'NY Open':h<12?'AM':h<16?'PM':'After-hours'}
function timingPanel(rows){const timed=rows.map(x=>({...x,ny:nyParts(x.entry)})).filter(x=>x.ny),day=aggregate(timed,x=>x.ny.day),hour=aggregate(timed,x=>String(x.ny.h).padStart(2,'0')+':00'),sess=aggregate(timed,x=>session(x.ny.h)),best=a=>[...a].filter(x=>x.n>=2).sort((x,y)=>y.exp-x.exp)[0],bd=best(day),bh=best(hour),bs=best(sess);return`<section class="an40-card an40-timing"><h3>Timing Edge</h3><div>${kpi('Best Day (NY)',bd?.key||'Need more data',bd?`${money(bd.exp)} expectancy · ${bd.n} trades`:'')}${kpi('Best Hour (NY)',bh?.key||'Need more data',bh?`${money(bh.exp)} expectancy · ${bh.n} trades`:'')}${kpi('Best Session',bs?.key||'Need more data',bs?`${money(bs.exp)} expectancy · ${bs.n} trades`:'')}</div></section>`}
function edgePanel(rows,m){const largest=m.gp?Math.max(0,...rows.filter(x=>x.p>0).map(x=>x.p))/m.gp*100:0;let score=0;score+=Math.min(25,m.n/50*25);score+=Math.max(0,Math.min(25,((Number.isFinite(m.pf)?m.pf:3)-1)/1.5*25));score+=m.exp>0?20:0;score+=20*(1-Math.min(1,largest/70));score+=10*(1-Math.min(1,m.maxDD/Math.max(1,Math.abs(m.net)+m.maxDD)));score=Math.round(score);return`<section class="an40-card an40-edge"><div><h3>Edge Evidence</h3><p>Evidence quality, not a guarantee of future results.</p></div><div class="an40-edge-score"><b>${score}</b><span>/100</span></div><div class="an40-edge-facts"><span>Sample <b>${m.n}</b></span><span>Profit Factor <b>${Number.isFinite(m.pf)?m.pf.toFixed(2):'∞'}</b></span><span>Expectancy <b>${money(m.exp)}</b></span><span>Largest-win concentration <b>${pct(largest)}</b></span></div></section>`}
function unverified(rows){const n=rows.filter(x=>!x.entry).length;return n?`<div class="an40-alert">${n} closed trade${n===1?'':'s'} have no verified entry time. Timing analytics exclude them.</div>`:''}
function renderHtml(state,f){const all=allRows(state),rows=applyFilters(all,f),m=stats(rows),inst=aggregate(rows,x=>x.t.instrument||'Unknown'),strategies=aggregate(rows,x=>x.strategy),companies=(state.companies||[]).filter(c=>(state.accounts||[]).some(a=>a.companyId===c.id)),instruments=[...new Set(all.map(x=>x.t.instrument).filter(Boolean))].sort(),strategyList=[...new Set(all.map(x=>x.strategy).filter(Boolean))].sort(),bounds=periodBounds(f);return`<section class="an40-page" data-analytics-v40><div class="an40-head"><h1>Advanced Analytics</h1><span>${rows.length}/${all.length} closed trades</span></div><div class="an40-filters"><label>Scope<select data-an40-filter="company"><option value="ALL">All Accounts</option>${companies.map(c=>`<option value="${esc(c.id)}">${esc(c.name)}</option>`).join('')}</select></label><label>Period<select data-an40-filter="period"><option value="ALL">All Time</option><option value="7D">7 Days</option><option value="30D">30 Days</option><option value="90D">90 Days</option><option value="YTD">YTD</option><option value="CUSTOM">Custom Range</option></select></label><label>From<input type="date" data-an40-filter="from" value="${esc(f.period==='CUSTOM'?f.from:bounds.from)}" ${f.period==='CUSTOM'?'':'disabled'}></label><label>To<input type="date" data-an40-filter="to" value="${esc(f.period==='CUSTOM'?f.to:bounds.to)}" ${f.period==='CUSTOM'?'':'disabled'}></label><label>Instrument<select data-an40-filter="instrument"><option value="ALL">All</option>${instruments.map(v=>`<option>${esc(v)}</option>`).join('')}</select></label><label>Direction<select data-an40-filter="direction"><option value="ALL">All</option><option>LONG</option><option>SHORT</option></select></label><label>Strategy<select data-an40-filter="strategy"><option value="ALL">All</option>${strategyList.map(v=>`<option>${esc(v)}</option>`).join('')}</select></label><button type="button" data-an40-reset>Reset</button></div>${unverified(rows)}<div class="an40-kpis">${kpi('Net P&L',money(m.net),'Master trade / account',m.net>=0?'good':'bad')}${kpi('Total Trades',String(m.n),'Closed master trades')}${kpi('Win Rate',pct(m.wr),`${m.w}W / ${m.l}L / ${m.be} BE`,m.wr>=50?'good':'')}${kpi('Profit Factor',Number.isFinite(m.pf)?m.pf.toFixed(2):'∞','Gross win ÷ loss',m.pf>=1?'good':'bad')}${kpi('Expectancy',money(m.exp),'Per master trade',m.exp>=0?'good':'bad')}${kpi('Max Drawdown',money(m.maxDD),'Master equity','bad')}${kpi('Avg Win',money(m.aw),'Winning trades','good')}${kpi('Avg Loss',money(m.al),'Losing trades','bad')}${kpi('Avg Duration',fmtDuration(m.avgDuration),'Verified timestamps')}</div><div class="an40-chart-row"><section class="an40-card an40-equity"><div class="an40-card-head"><h3>Equity Curve</h3><span>${money(m.net)}</span></div>${lineSvg(m.points)}</section><section class="an40-card"><div class="an40-card-head"><h3>Monthly Performance</h3></div>${monthlyBars(rows)}</section><section class="an40-card"><div class="an40-card-head"><h3>Win Rate & Profit Factor</h3></div>${comboSvg(rows)}</section></div><div class="an40-detail-row">${donut('Instrument Breakdown',inst,m.n)}${donut('Strategy Breakdown',strategies,m.n)}${dailyTable(rows)}${tradesTable(rows)}</div><div class="an40-bottom-row">${edgePanel(rows,m)}${timingPanel(rows)}</div></section>`}
function bind(root,state,f){root.querySelectorAll('select[data-an40-filter]').forEach(el=>{const key=el.dataset.an40Filter;if(key in f)el.value=f[key];el.onchange=()=>{const n={...f,[key]:el.value};if(key==='period'&&el.value!=='CUSTOM'){n.from='';n.to=''}saveFilters(n);lastSignature='';render()}});root.querySelectorAll('input[data-an40-filter]').forEach(el=>{const key=el.dataset.an40Filter;el.onchange=()=>{const n={...f,[key]:el.value,period:'CUSTOM'};saveFilters(n);lastSignature='';render()}});root.querySelector('[data-an40-reset]')?.addEventListener('click',()=>{saveFilters(defaults());lastSignature='';render()});root.querySelectorAll('[data-an40-tab]').forEach(btn=>btn.onclick=()=>{root.querySelectorAll('[data-an40-tab]').forEach(x=>x.classList.toggle('active',x===btn));root.querySelector('[data-an40-best]')?.classList.toggle('hidden',btn.dataset.an40Tab!=='best');root.querySelector('[data-an40-worst]')?.classList.toggle('hidden',btn.dataset.an40Tab!=='worst')})}
function render(){if(route()!=='analytics'){document.querySelectorAll('.page.an40-mounted').forEach(p=>p.classList.remove('an40-mounted'));return}const host=document.querySelector('#app .page');if(!host)return;const state=loadState(),f=loadFilters(),sig=`${state.meta?.updatedAt||''}|${JSON.stringify(f)}`;let root=host.querySelector('[data-analytics-v40]');if(root&&lastSignature===sig)return;host.classList.add('an40-mounted');const wrap=document.createElement('div');wrap.innerHTML=renderHtml(state,f);const fresh=wrap.firstElementChild;if(root)root.replaceWith(fresh);else host.appendChild(fresh);lastSignature=sig;bind(fresh,state,f)}
function schedule(){clearTimeout(renderTimer);renderTimer=setTimeout(render,60)}
window.addEventListener('load',schedule);window.addEventListener('hashchange',()=>{lastSignature='';schedule()});window.addEventListener('storage',e=>{if(e.key===STATE_KEY){lastSignature='';schedule()}});new MutationObserver(schedule).observe(document.documentElement,{childList:true,subtree:true});schedule();
